{{--
    Backoffice footer. The original PreSkool pages have no visible footer,
    so this is a minimal, Bootstrap-only addition that does not alter the
    theme design. Remove or extend freely.
--}}
<div class="d-sm-flex align-items-center justify-content-between border-top bg-white p-3 mt-auto">
    <p class="mb-0 text-muted">&copy; {{ now()->year }} {{ config('app.name') }}</p>
    <p class="mb-0 text-muted">GLS SPRACHENZENTRUM BACKOFFICE</p>
</div>
